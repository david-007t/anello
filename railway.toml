[build]
builder = "nixpacks"
buildCommand = "pip install -r pipeline/requirements.txt && playwright install chromium --with-deps"
watchPatterns = ["pipeline/**", "requirements.txt"]

[deploy]
startCommand = "cd pipeline && uvicorn api:app --host 0.0.0.0 --port 8000"
restartPolicyType = "always"
